mainView = {
    type = "view"
    size = function(parentSize)
    {
        return parentSize
    }
    subviews = {
        {
            id = "pageTitle"
            type = "text"
            font = "roboto"
            fontSize = 24
            alignmentY = "above"
            layoutParentID = "pageBody"
        }
        {
            id = "pageBody"
            type = "text"
            font = "roboto"
            fontSize = 18
            size = function(parentSize)
            {
                return vec2(parentSize.x - 20, parentSize.y - 80)
            }
        }
    }
}