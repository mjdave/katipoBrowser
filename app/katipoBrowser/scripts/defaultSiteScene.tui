# NOTE this is used in two places, both when there is a simple result, like an error with a message, and when displaying a basic tui markup document .tml site

mainView = {
    type = "view"
    subviews = {
        {
            id = "pageBody"
            type = "markup"
        }
    }
}